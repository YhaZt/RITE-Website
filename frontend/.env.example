# RITE frontend — copy to .env and adjust for your local API
# Local Vite + artisan serve:
VITE_API_BASE_URL=http://127.0.0.1:8000

# Production example (same origin or reverse-proxied API):
# VITE_API_BASE_URL=https://api.example.com
